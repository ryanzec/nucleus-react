import * as issuesData from './issues';

export const testProjectIssues = [
  issuesData.story,
  issuesData.task,
  issuesData.bug,
  issuesData.opened,
  issuesData.inProgress,
  issuesData.closed,
];
